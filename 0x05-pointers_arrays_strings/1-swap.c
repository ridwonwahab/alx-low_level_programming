#include "main.h"

/**
 *swap_int - this method swaps the values of two ints passed to it
 *@a: the first int passed
 *@b: the second int passed
 */
void swap_int(int *a, int *b)
{

	int z;

	z = *a;
	*a = *b;
	*b = z;
		}
